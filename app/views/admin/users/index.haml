.page-header
  %h2 Manage Users
  = link_to 'Create User', new_admin_user_path, class: 'btn btn-success'

.row
  .col-md-12
    %table.table.table-hover
      %h3 Users
      %tbody
        %tr
          %th Role
          %th Email
          %th First Name
          %th Last Name
          %th Group ID
          %th Show
          %th Edit
          %th Delete
        - @users.each do |user|
          %tr
            %td
              = user.role
            %td
              = user.email
            %td
              = user.first_name
            %td
              = user.last_name
            %td
              = user.group_id
            %td
              = link_to "Show", admin_user_path(user), class: "btn btn-success"
            %td
              = link_to "Edit", edit_admin_user_path(user), class: "btn btn-warning"
            %td
              = link_to "Delete", admin_user_path(user), method: :delete, class: "btn btn-danger", confirm: "Are you sure? There is no undoing this."
